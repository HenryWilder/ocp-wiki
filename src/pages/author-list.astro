---
import ArticleLayout from '../layouts/ArticleLayout.astro';
import type { IAuthor } from '../scripts/index';
import AuthorCard from '../components/AuthorCard.astro';
import authors from '../../public/data/authors.json';
const authorList: IAuthor[] = authors;
---

<ArticleLayout title="Authors" name="Author List" desc="Obscure, Classify, Preserve">
<ul>
	{authorList.map(author => <AuthorCard authorData={author} />)}
</ul>
</ArticleLayout>
